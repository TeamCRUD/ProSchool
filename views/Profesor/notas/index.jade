extends ../layout_app

block content_app
    .list-content
        .list-title
            h1 Lista de estudiantes #[b.hideL / #{user.grade}]
        for student in students
            if(student.typeuser == 'Estudiante')
                .list-main
                    .list-main-title
                        if(student.typeuser == 'Estudiante')
                            a(href="/app/#{student.username}/")=student.fullname
                    .list-main-item
                        a(href="/app/task/#{student.username}") Ver mas