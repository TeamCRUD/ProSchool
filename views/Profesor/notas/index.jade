extends ../layout_app

block content_app
    .list-content
        .list-title
            h1 Lista de estudiantes / #{user.grade}
        for student in students
            .list-main
                .list-main-title
                    if(student.typeuser == 'Estudiante')
                        a(href="/app/#{student.username}/")=student.fullname 