extends ../layout_app

block content_app
    .show-content 
        .show-main
            h1= nota.task + ' : ' + nota.note
            if(typeof nota.profesor != "undefined")
                p Por @#{nota.profesor.username} | Estudiante: @#{nota.student}