extends ../layout_app

block content_app
    h1 Subir nueva nota
    form(action="/app/notas", method="POST")
        div
            input(type="text", name="periodo", placeholder="Periodo", id="periodo")
        div
            input(type="text", name="nota", placeholder="Escribir nota", id="nota")
        div
            input(type="submit" value="Subir")
        div
            a(href="/app") Cancelar