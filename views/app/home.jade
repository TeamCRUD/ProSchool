extends layout_app

block content_app
    div
        for nota in notas
            h1= nota.periodo + ' ' + nota.nota
            if(typeof nota.profesor != "undefined")
                p Por #{nota.profesor.name  + ' ' + nota.profesor.lastname }